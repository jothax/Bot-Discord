#include <iostream>
#include <dpp/dpp.h>
#include <cpr/cpr.h>
#include <src/include/request.h>

int main() 
{ 
    //UNDER CONSTRUCTION
    // dpp::cluster bot("");
 
    // bot.on_log(dpp::utility::cout_logger());
 
    // bot.on_slashcommand([](const dpp::slashcommand_t& event) {
    //     if (event.command.get_command_name() == "cep") {
    //         cpr::Response r =request::cep(72649715);
    //         event.reply(r.text);
    //     }
    // });

    return 0;  
}